[package]
name = "c-api"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "shiguredo_toml_c"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
shiguredo_toml = { path = "../../" }

[build-dependencies]
cbindgen = { version = "0.29", default-features = false }
