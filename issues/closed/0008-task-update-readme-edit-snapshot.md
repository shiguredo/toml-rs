# 0008-task-update-readme-edit-snapshot

## 背景

`README.md` に edit API と edit 向け snapshot テスト手順の記載が不足している。

## 対応内容

- `非破壊編集` セクションに主な API 一覧を追記する
- `toml-test` セクションに `make toml-test-ci` を追記する
- `edit 向け snapshot テスト` セクションを追加する

## 完了条件

- `README.md` に上記 3 点が反映されている

## 完了内容

- `非破壊編集` セクションへ edit API の一覧を追記した
- `toml-test` セクションへ `make toml-test-ci` を追記した
- `edit 向け snapshot テスト` セクションを追加した
