[package]
name = "pbt"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
# プロパティベーステスト（シードとケース数を呼び出し側が明示する imperative な PBT）
noprop = "0.2"
# 被テストクレート
shiguredo_toml = { path = "../" }
